# Palette's Journal

## 2025-02-20 - Accessibility in Component Libraries
**Learning:** Even in modern React codebases, basic semantic associations (label + input) are often missed when focusing on visual styling.
**Action:** Always check form inputs for `id` and matching `htmlFor` on labels, regardless of how "modern" the UI looks.
